import styled from '@emotion/styled'
import { space, color, fontSize, maxWidth, width } from 'styled-system'

export const Box = styled('div')(space, maxWidth, width, color)
export const Heading = styled('h1')(space, color, fontSize)
