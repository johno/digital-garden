import styled from '@emotion/styled'
import { fontSize, space, maxWidth, width } from 'styled-system'

export const Box = styled('div')(space, maxWidth, width)
export const Heading = styled('h1')(space, fontSize)
